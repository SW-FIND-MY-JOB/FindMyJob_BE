spring:
  application:
    name: cover-letter-service
  cloud:
    config:
      uri: http://config:8888
      name: coverletterservice
      retry:
        max-attempts: 10
        max-interval: 2000
        initial-interval: 1000