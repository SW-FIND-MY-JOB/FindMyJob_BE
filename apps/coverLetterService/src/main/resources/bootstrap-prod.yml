spring:
  application:
    name: cover-letter-service
  cloud:
    config:
      uri: http://localhost:8888
      name: coverletterservice
      retry:
        max-attempts: 10
        max-interval: 10000
        initial-interval: 1000