spring:
  application:
    name: job-service
  cloud:
    config:
      uri: http://localhost:8888
      name: jobservice

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka