spring:
  application:
    name: job-service
  cloud:
    config:
      uri: http://localhost:8888
      name: jobservice
      retry:
        max-attempts: 10
        max-interval: 10000
        initial-interval: 1000