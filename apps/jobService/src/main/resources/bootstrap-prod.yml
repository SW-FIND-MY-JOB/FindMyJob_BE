spring:
  application:
    name: job-service
  cloud:
    config:
      uri: http://config:8888
      name: jobservice
      retry:
        max-attempts: 10
        max-interval: 2000
        initial-interval: 1000