spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://config:8888
      name: authservice
      retry:
        max-attempts: 10
        max-interval: 2000
        initial-interval: 1000