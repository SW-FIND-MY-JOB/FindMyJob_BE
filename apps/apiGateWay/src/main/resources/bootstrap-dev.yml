spring:
  application:
    name: apigateway
  cloud:
    config:
      uri: http://localhost:8888
      name: apigateway

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka