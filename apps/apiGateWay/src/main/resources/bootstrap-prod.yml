spring:
  application:
    name: apigateway
  cloud:
    config:
      uri: http://172.25.0.154:8888
      name: apigateway
      retry:
        max-attempts: 10
        max-interval: 10000
        initial-interval: 1000