spring:
  application:
    name: apigateway
  cloud:
    config:
      uri: http://localhost:8888
      name: apigateway
      retry:
        max-attempts: 10
        max-interval: 10000
        initial-interval: 1000