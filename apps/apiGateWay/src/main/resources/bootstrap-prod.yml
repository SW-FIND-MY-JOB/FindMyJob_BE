spring:
  application:
    name: apigateway
  cloud:
    config:
      uri: http://config:8888
      name: apigateway
      retry:
        max-attempts: 10
        max-interval: 2000
        initial-interval: 1000